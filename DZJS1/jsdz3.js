let y = +prompt("Введите первое число координат");
let x = +prompt("Введите второе число координат");
        if (x > 0 && y > 0) {
            console.log ("вы находитесь в 1-ой четверти")
        }
        else if (!x) {
            console.log ("вы ввели не корректные данные")
        }
        else if (!y) {
            console.log ("вы ввели не корректные данные")
        }
        else if 
         (x < 0 && y > 0) {
          console.log ("вы находитесь в 2-ой четверти")
        }
        else if (!x) {
            console.log ("вы ввели не корректные данные")
        }
        else if (!y) {
            console.log ("вы ввели не корректные данные")
        }
        else if 
         (x < 0 && y < 0) {
          console.log ("вы находитесь в 3-ой четверти")
        }
        else if (!x) {
            console.log ("вы ввели не корректные данные")
        }
        else if (!y) {
            console.log ("вы ввели не корректные данные")
        }
         else if 
         (x > 0 && y < 0) {
          console.log ("вы находитесь в 4-ой четверти")
        }
        else if (!x) {
            console.log ("вы ввели не корректные данные")
        }
        else if (!y) {
            console.log ("вы ввели не корректные данные")
        }